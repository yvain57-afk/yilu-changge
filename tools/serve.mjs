import {createServer} from 'node:http';import {readFile,stat} from 'node:fs/promises';import {resolve,extname} from 'node:path';
const base=resolve('build/web-mobile');const types={'.html':'text/html','.js':'text/javascript','.json':'application/json','.png':'image/png','.css':'text/css','.wasm':'application/wasm','.wav':'audio/wav','.mp3':'audio/mpeg'};
createServer(async(req,res)=>{try{const p=resolve(base,'.'+decodeURIComponent(new URL(req.url,'http://127.0.0.1').pathname));if(!p.startsWith(base+'/')&&p!==base)throw Error('path');const file=(await stat(p)).isDirectory()?resolve(p,'index.html'):p;res.writeHead(200,{'Content-Type':types[extname(file)]||'application/octet-stream','Cache-Control':'no-store'});res.end(await readFile(file));}catch{res.writeHead(404);res.end('Not found');}}).listen(43187,'127.0.0.1',()=>console.log('Cocos build only: http://127.0.0.1:43187'));

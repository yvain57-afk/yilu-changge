import {chromium} from 'playwright';import assert from 'node:assert/strict';import {writeFileSync} from 'node:fs';
const root='evidence/v02-s2b';const c=await chromium.launchPersistentContext('.cache/browser-s2b-audio',{executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:false,viewport:{width:390,height:844},hasTouch:true,isMobile:true,args:['--disable-backgrounding-occluded-windows']});
// Mirror the real WebAudio output into a recording destination. No microphone,
// replacement sounds, time control, or simulation mutation.
await c.addInitScript(()=>{const connect=AudioNode.prototype.connect;const buses=new Map();globalThis.__audioCapture={buses,connections:0};AudioNode.prototype.connect=function(destination,...rest){const result=connect.call(this,destination,...rest);if(destination===this.context.destination){let bus=buses.get(this.context);if(!bus){bus=this.context.createMediaStreamDestination();buses.set(this.context,bus);}connect.call(this,bus);globalThis.__audioCapture.connections++;}return result;};});
const p=await c.newPage(),errors=[],samples=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:43187/');await p.bringToFront();await p.waitForFunction(()=>globalThis.__YLCG__?.snapshot().version==='v02-s2b');const snap=()=>p.evaluate(()=>globalThis.__YLCG__.snapshot());const cd=await c.newCDPSession(p);
async function pos(x,y){const r=await p.locator('canvas').boundingBox(),s=r.width/720;return{x:r.x+r.width/2+x*s,y:r.y+r.height/2-y*s,s};}async function tap(id){const b=(await snap()).buttons.find(b=>b.id===id),xy=await pos(b.x,b.y);await p.touchscreen.tap(xy.x,xy.y);await p.waitForTimeout(100);}
let cursor=195,touch=false;async function release(){if(touch)await cd.send('Input.dispatchTouchEvent',{type:'touchEnd',touchPoints:[]});touch=false;}async function target(x){const j=(await snap()).journey,xy=await pos(0,-160);if(!touch){cursor=xy.x;await cd.send('Input.dispatchTouchEvent',{type:'touchStart',touchPoints:[{x:cursor,y:xy.y,id:1}]});touch=true;}const next=cursor+(x-j.target)*260*xy.s;if(next<15||next>375){await release();return target(x);}cursor=next;await cd.send('Input.dispatchTouchEvent',{type:'touchMove',touchPoints:[{x:cursor,y:xy.y,id:1}]});}
function steer(j){if(j.phase==='boss'){const w=j.bossWarning;return w&&!w.hit&&Math.abs(w.x)<=w.width?(w.x<=0?.78:-.78):0;}const r=j.rows.find(r=>r.at-j.z<=7);let x=0;if(r)x=r.left.kind==='double'?(j.count>=r.right.value?-.5:.5):(r.left.value>=j.count?-.5:.5);const o=j.obstacles.find(o=>o.rowId===undefined&&o.at-j.z<1&&o.at>j.z&&Math.abs(o.x-x)<=o.width);if(o)x=o.x<=0?.8:-.8;return x;}
let report={passed:false};try{
 await p.evaluate(()=>localStorage.removeItem('yilu-changge-prototype-v2'));await p.reload();await p.waitForFunction(()=>globalThis.__YLCG__?.snapshot().version==='v02-s2b');
 await p.waitForFunction(()=>globalThis.__audioCapture.buses.size>0);const capture=await p.evaluate(()=>{const canvas=document.querySelector('canvas'),bus=Array.from(globalThis.__audioCapture.buses.values())[0],stream=canvas.captureStream(25);bus.stream.getAudioTracks().forEach(t=>stream.addTrack(t));const chunks=[],rec=new MediaRecorder(stream,{mimeType:'video/webm;codecs=vp9,opus',videoBitsPerSecond:3000000});globalThis.__recording={rec,chunks};rec.ondataavailable=e=>chunks.push(e.data);rec.start(1000);return{mime:rec.mimeType,audioTracks:stream.getAudioTracks().length,connections:globalThis.__audioCapture.connections};});
 const start=Date.now();await p.waitForTimeout(1000);await tap('start');
 while((await snap()).screen==='battle'){const s=await snap(),j=s.journey;await target(steer(j));samples.push({elapsed:j.elapsed,count:j.count,x:j.x,hp:j.bossHP,phase:j.phase});assert.ok(Date.now()-start<180000);await p.waitForTimeout(80);}
 await release();const result=await snap();assert.equal(result.journey.phase,'won');assert.equal(result.save.cleared['trial-01'],true);await p.waitForTimeout(2400);
 const base64=await p.evaluate(async()=>{const{rec,chunks}=globalThis.__recording;await new Promise(resolve=>{rec.onstop=resolve;rec.stop();});const bytes=new Uint8Array(await new Blob(chunks).arrayBuffer());let binary='';for(let i=0;i<bytes.length;i+=32768)binary+=String.fromCharCode(...bytes.subarray(i,i+32768));return btoa(binary);});writeFileSync(`${root}/actual-audio.webm`,Buffer.from(base64,'base64'));assert.deepEqual(errors,[]);report={passed:true,version:result.version,capture,samples,result:result.journey,errors,audio:'actual browser WebAudio mirrored live; no added soundtrack',scope:'S2-B normal first-level play, home through saved promotion; uncut real-time recording',wallSeconds:(Date.now()-start)/1000};
}catch(e){report={...report,error:String(e),errors,samples};process.exitCode=1;}finally{writeFileSync(`${root}/audio-capture.json`,JSON.stringify(report,null,2));await c.close();console.log(JSON.stringify({...report,samples:samples.length}));}

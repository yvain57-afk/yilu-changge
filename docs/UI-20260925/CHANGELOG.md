# UI-20260925 实际改动

在本地 v0.9 未提交工作基础上修改；运行标识 ui-20260925，交付包版本0.9.1-ui20260925。没有恢复音乐、固定倍增选门、难易分路、击杀30/80升级或强制近战。

## 画面
- 首页接入独立无字城寨底图、书法字标与蓝披风主将。主将按实际长枪/长刀选择切换独立立绘。
- 关卡用完整三关山川地图，真实节点/锁定条件/目标/成绩独立绘制。没有示意图货币、星级、第六章和额外武将。
- 整备用两个角色卡槽展示主将与至多一名真实同袍，所有可解锁同袍及两种兵器可查看。
- 营地采用静态驻地，结果采用独立战旗背景。真正透明金色主按钮用9-slice缩放；墨蓝面板为可伸缩引擎组件。
- 战斗木旗门、箱体木纹与金属箍、裂纹和实际改值反馈；三类路边装饰与开局可见远城；射击动画由真实发射事件驱动。

## 交互与存档
- 关卡点击仅选择，独立按钮出征；未完成的过渡优先。
- 整备独立出征/继续/驻地动作、返回来源、锁定原因；短屏中部滚动，选择保留滚动位置，标题和底部按钮固定。
- 点击态反馈、禁用态、忙碌态；跨页松手不透传；所有页面按钮至少48逻辑像素，旗帜与落点也使用同尺度。
- Book/Growth/Campaign写入返回真实成败；成功清除错误，失败保留内存和原磁盘内容，结果/选择/过渡提供重试；不会因重复渲染重新发奖。
- 启用Cocos Mask模块用于真正裁剪滚动区；没有升级引擎或改存档key/schema。

## 资源与规则边界
8项新生产资源共868,156字节；生成原图和压缩记录保留。旧图集仍对所有使用帧及4px边界进行RGBA逐像素保护。已有角色头像和战斗图集复用，不宣称全套新肖像/动作已绘制。
核心runner、关卡显式初值、模型、武器规则与投影逐文件比对基线无改动，见RULES_PRESERVATION.json。

## 已知未完成
详见KNOWN_GAPS.md。整体视觉验收不标通过，手机真机不标通过。

# 直接发给当前Codex的执行指令

请直接实施本包，不再输出一份新策划后停下。

继续 `yvain57-afk/yilu-changge` 当前最新本地工作区。核验远端参考为
`a8b56c16cc94f26d81fe12479b3d12854989efd8`；本地更新或未提交修改必须保留。
原授权工程目录为 `/Users/yvainair/Code/游戏-左右滑古代史`；以实际绑定目录、AGENTS.md和权限为准，不创建第二套游戏，不换引擎，不reset/clean/stash未知工作。

本轮完成《一路长歌：三国》v0.4：主角改为持枪武将，后排弓兵保留真实齐射；已有三局变为“乱军突围—夺粮立营—白石解围”；用同一营地/军略过渡组件完成整军、建营、驻防，连成能从头玩到尾的一段旅程。

优先读 README_FIRST.md，再按读取顺序实施。

关键约束：
- 正常游戏页面、教程、加载、结算不出现“架空演绎”“草模”“试制”“非史实”等出戏提示。不出现真实年份。开发说明只留在文档及显式调试页。保留必要版权署名、隐私及平台要求，不能随本条删除。
- 主角身份是你，无固定姓名、无穿越、无修书、无纸兵/墨影，不继续使用主角射箭动画或刀光波冒充近战。
- 长枪要有真实攻击范围、前摇/有效段/恢复。主角从弓兵数量中剔除；弓兵伤害只按N-1计，主将另算，剩1人仍可接敌获胜。
- 接续并保留Volley、WorldScenery、六帧步态、既有三关流程和授权音乐。普通友軍不退回单一中心箭；场景不退回整排取模回跳。
- 只制作一把枪、三关、一个共用过渡组件和一次赵云战后会面；不做32关、全武将、装备背包、经营、抽卡和第四关。
- 按包内机制显式迁移受影响测试。旧射箭纯数值断言与新版近战合同冲突时保留旧fixture，新增新版断言和变更说明；不能删除失败用例或放宽门、公平性、存档边界。
- 单位数、受阻门一次事务、预告可见、无硬超时、失败免费重开保留。存档不清空。
- 本轮授权对“原地远程Boss”改为模型中的接近交锋位置，并按合同调整投射物/攻击起点和距离，不能只在画面里把Boss拉近。
- 内部按战斗、美术、过渡、整合顺序连续完成。没有付费或权限阻塞，不逐项向用户求确认。
- 不默认运行 tools/verify-v03.mjs、历史十局长测或游客微信重复构建。局部检查完成后一次自然三关含音轨录像复用为流程证据；纯规则单人可通路线可加速模拟，不占用十局实时试玩。

开工回复不超过8行：当前基线、保留项、这一批实际改动和首个可玩的里程碑。完整日志写本地，只在失败时展示相关段落。第一次让新近战在Cocos里打中敌人后继续完成三关，不在那里结束交付。

最终交付：真实可玩构建、已验证启动入口、从首页经两次过渡到白石驻地的连续原速有声录像、同版本局部/快速检查摘要和真实缺项。不称已通过用户审美或微信真机验收。未经授权不推送/部署/上架。

# 一路长歌：三国 v0.4 交付复核

继续当前工程与已认可的主角方向；没有回退旧 S1，没有覆盖此前微信分包及运行时修复。包内 12 项 manifest 校验通过，规格原文存于 package/。

## 本轮实现

- 唯一持枪主将固定 5 点近战，10/5/21 tick 准备、有效、恢复；每次最多命中一目标，山石挡枪，门侧木障按整半侧处理。近战读取实时横向位置，不卡输入。
- N 人由 1 主将与 N−1 弓兵构成。每 0.25 秒齐射，伤害冻结于发射；1 人不产生新箭。可见 47 名弓兵，最多 5 组、235 支表现箭，原有射击生命周期保留。
- 三位 Boss 从 depth 4 于 60 tick 接近到 .9，首个预告仍在 2 秒；碰撞采用箭与移动 Boss 的相对扫掠。脚底、枪尖、箭终点和受击共用投影。原有 Boss HP、攻击模式、损兵不变。
- 三关改为乱军突围、夺粮立营、白石解围；首关增加 id 901、3 秒位置、14 HP 敌兵，让默认中央路线能实际接敌出枪。S2 木障损失 5 保留。
- 一个共用过渡组件，三种结果：12 人先锋整军、16 人先锋建营出征、白石落旗驻防。拖旗、点目标、主按钮走同一个幂等入口；无强制读秒、金币或经营系统。
- 赵云只在白石会面；驻地可重玩三关，没有第四关或空武将/装备菜单。
- 基础存档及原键不变；sidecar `yilu-changge-campaign-v04` 保存过渡/会面状态。旧三关存档直接迁移，已完成过渡不重复强制播放；待完成过渡可续接。
- 四关键上身姿态 + 独立长枪 + 原六帧腿部，模型采样相位。素材、提示词、透明度与锚点记录见 art-source/v04/。
- 原 Shenyang 音乐、5 路现有效果音及授权署名保留。没有重做配乐。

## 验证与历史测试处理

- 78 项快速测试通过，包含新增 14 项近战/sidecar 场景；类型检查通过。
- 原 v031 的 N×0.2、固定 Boss depth 和旧称谓断言与本轮明确规则冲突。旧文本完整保存在 tests/fixtures/v031/，原 S1/v03 模型 fixture 未删除。表现层测试改为验证表现消费者不会改变 v04 的逐 tick 模型/事件，不再错误要求 v04 等于旧 DPS。
- 三个真实配置的单主将 Boss 模型测试均可通过：约 97.8 / 219.5 / 316.7 秒，没有隐藏增兵、箭或超时自动胜利。普通/安全/晚切路线仍全部可胜。
- 360×780、390×844、360×640 合成短场景覆盖三 Boss、三过渡、赵云会面与驻地，点击/拖动等价、取消拖放、旧触摸抬起不误触、后台返回明确继续。
- 48/256 人各 5 秒短密度测试（首秒预热排除）：p99 均约 17.7 ms，无超过 33.34 ms 帧。三次重开池/节点/GPU buffer 稳定。这是本机 Chrome 结果，不是手机性能承诺。
- 没有运行 verify-v03.mjs 或历史十局浏览器长测；保留了不到一秒执行完成的纯模型规则套件。

## 微信边界

已按当前源码构建 wechatgame，并恢复本地现有小游戏测试身份；不把测试身份写入公开源码，不使用唯评有数或 Golf 业务小程序身份。
开发者工具状态：已能连接，skill 0.3.9 一致、无需 CLI token，但微信登录过期。已打开官方扫码登录窗口，10 次轮询后仍 pending。未上传 v0.4 预览，未生成新游戏预览二维码，未宣称真机通过。详见 evidence/v04/wechat-status.json；恢复时先查询旧 login taskId，勿重复发起登录/上传。

## 交付与证据

浏览器入口 http://127.0.0.1:43187/play/；本工程根目录“启动一路长歌.command”复用同一工程服务。完整录像、自然通关数据、构建结果、代码和产物指纹集中于 evidence/v04/。功能通过不等于用户画风/手感验收，最后由用户集中试玩。

## 最终自然录像结果

一次连续真实触控三关、原速采集，222.084 秒，390×844，H.264 + AAC 48kHz 双声道。期间短暂停一次以验证画面/声音暂停恢复；无模拟跳关、改兵力或改 HP。

|关卡|模拟用时|结余兵力|近战命中次数|
|---|---:|---:|---:|
|1|56.32s|88|16|
|2|66.07s|204|19|
|3|80.82s|256|23|

基础胜利先保存；所有拖旗过渡、会面、驻地均出现在同一录像，录制结束后刷新验证基础档与 sidecar 一致。浏览器 pageerror 为空。短摘录只从完整录像提取 1–7、44–53、59–63、215–222 秒，未另跑三关。

运行源码指纹：`a37d7e8f9c270a09b3f229232ac03d018742fd110ad09994e0d81d1df767db56`。构建包及逐文件指纹见 evidence/v04/fingerprint.json；测试、原始 WebM 与发布 MP4 均留存。最终 Boss 血条布局调整后补跑 360/390 三 Boss 短检查，详见 layout-final.json。

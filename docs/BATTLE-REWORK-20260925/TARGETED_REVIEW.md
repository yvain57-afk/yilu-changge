# 最终构建定向检查

本页全部是明确标注的隔离 fixture：使用现有 Journey 构造参数，真实自动出弹、模型推进、接触和奖励事务。未把任何定向截图称为自然流程。构建：battle-rework-20260925，390×844；短屏 375×667。

22 项断言全部通过，浏览器错误 0。四类箱分别检查完整→首次受击→击破，耐久 3→2→0，奖励只兑现一次，永久成长不被临时奖励污染。兵员、兵器、连续门、援手箱具有不同箱体和开启姿态，全部 12 张实际截图已查看。援手赵云来自真实成员快照。

可射击门实际经历 -2→0→+1→封顶 +2，零值灰色、负值红色、正值蓝色，封顶后显示“已满”；额外命中不突破上限，穿越才兑现。连续 +1 实取 3 个后横移，余下 3 个错过而不补发。归零直接失败，后方正门不能复活。

1、8、24、48、256 人在真实桌面浏览器各短运行约 1.8 秒：成员位置、权重、射击预算受限；256 人用 48 个加权成员显示，不能理解为绘制了 256 人。最大节点 535、池 501、峰值子弹 186，64 名在场敌人；最高 draw calls 249。p95 为 18.0–18.6 ms。24 人样本 112 帧中有 1 帧超过 33 ms；其他样本没有。引擎报告 GPU buffers 最高 297,378,644 字节、GPU textures 107,499,000 字节，均为引擎计数，不能当作物理手机内存实测。短采样不证明长期无泄漏或真机性能。

375×667 实际触摸取消后横移停止；暂停时模型与音效冻结，新触摸继续后时间推进；暂停按钮热区至少 48 逻辑像素且在安全区。截图已查看：HUD、队伍、目标可见。连续 +1 密集排列仍靠近，逐个数值可读；并未声称原版构图和公式完全复原。

证据：targeted-checks.json，targeted/ 内每项 PNG 与模型/视图 JSON。

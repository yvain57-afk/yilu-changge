# 直接执行｜一路长歌：三国 战斗 FIX2

任务：BATTLE-FIX2-20260925 · 文档1.0 · 2026-09-25

在当前本地最新累积工作区继续开发。已知上一份核验小包为 `0.9.2-battle20260925.1` / `battle-rework-20260925-fix1`；若本地已有更晚合法修改，保护它们并记录差异。任务名不等于已经完成的新游戏版本。

## 本轮要交付什么

完成真实战斗里的六组改动：武将攻击动作与兵器特有光波；门箱字体和信息布局；去掉顶部反复前移的装饰建筑；取消普通增援门过早封顶并检查增长窗口；美化中间隔离结构；统一所有目标标签和密集连续+1显示。

先读本文件，再读 `01_FIX2_IMPLEMENTATION.md`、`02_EVIDENCE_AND_CAP_AUDIT.md`、`03_ART_AND_LABEL_SPEC.md`、`04_ACCEPTANCE_AND_DELIVERY.md`。原话在05，参考及旧版实际画面在references。配置为实施初值，不是现有引擎API。

## 三处必须更新的旧约束

**武器身份与火力等级分开。** 近战持械武将按实际兵器发出对应光波；不把“基础弩/连弩/火矢”当作武将的三种兵器，不把所有普通兵都改成发波，也不新增兵器。

**允许对单门增长规则做本次有记录的修改。** 默认取消普通可改值门的低额正值封顶；真实命中伤害累计改值、接触领取一次、错过不补发仍保留。此项明确更新此前“关卡参数全部不动”的约束；不要只隐藏“满员”文字。数值安全护栏、逻辑人数容量、可见精灵数量必须分别核对。

**替换素材的标准是实际视觉。** FIX1报告已经写“核心战场素材齐备”；本轮是用户对其质量不认可后的重做。不能沿用更早报告的“素材未齐”作为当前结论，也不能以已有文件为由拒绝重画。

## 第一段工作

读取适用AGENTS、进度记录和本地差异，确认真实开关、角色兵器、门值配置、输入与标签路径。把映射写入 `FIX2_REALITY_MAP.md`，然后立即实施，不只返回计划。

已有证据提到的文件入口包括 BattleView.ts、Game.ts、ui/WorldLabels.ts、core/runner.ts、core/runnerConfig.ts；这些只是检索起点，必须在本地核实具体函数与调用链。小包没有完整源码，不得根据文件名编造行号。

## 内部执行顺序

先并行盘点动作素材与封顶参数；去掉顶部伪前景；接入一套光波动作、一个门、一个箱和一段隔离墙；用真实战斗样段核对，再扩展全三关与所有实际武将。阶段是内部检查点，无需每完成一个组件就等用户重新批准。

首页、地图、整备和战后只做必要兼容，不另起重构。不得改存档结构或删未知字段，不reset/clean/stash用户未知工作，不用远端旧工程覆盖本地。保留当前三关与自由横移，不恢复固定倍增核心、独立难易分路、30/80升级、强制近战。音乐保持关闭。

## 不接受的替代交付

仅换HUD、只改字体颜色、把箭涂亮当光波、整个人物图片旋转当攻击、把隔离线烘焙进不随世界定位的背景、隐藏封顶提示但保留原限制、静态贴图覆盖游戏、绿测试替代视觉对照，均不算完成。

## 最终交付

当前代码与可运行构建；30—45秒原速展示短片及四组同状态对照；原有自然三关完整流程；封顶旧值/新策略/依据及增长窗口对照；六项逐项结果。动效展示可从同一次自然录像剪接并交代切点；若缺少另一兵器或极端状态，补明确标注的定向片段，不能伪称都发生在同一局。

本包是开发规格和旧版证据参考，没有已修改的游戏代码，也没有新成品动作/贴图。缺关键素材时由开发端制作或明确具体阻塞；未达到目标的项保持未通过。

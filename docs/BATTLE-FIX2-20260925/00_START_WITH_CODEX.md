# FIX2 当前工程入口

版本 `0.9.3-battlefix2`，运行标识 `battle-fix2-20260925`。这是本地已有修改之上的工作树快照，不是 Git HEAD 副本。继续开发时保护现有修改及存档。

先看 `FIX2_REVIEW_RESULTS.json`、`VERIFICATION.json`、`KNOWN_GAPS.md` 与 `CAP_AND_PACING_REPORT.json`；以各项实际状态为准。开发过程中失败截图和尝试记录保留，不能把旧尝试当作最终版本，也不能把代码测试替代视觉审阅或手机实测。

完整开发包含源码、素材、Web/微信预构建。双击根目录 `启动一路长歌.command`，需 Node.js 18+，运行预构建不需要安装依赖或 Cocos。修改后构建使用 Cocos Creator 3.8.8。共享副本微信 AppID 置空，本机原配置不改；未上传或发布。

本轮范围：主将刀/枪、邢道荣斧、陈应叉、赵云枪的真实发射事件驱动动作与光波；四类箱单一信息组；正负零值与连续门本地排版；删除顶部成对装饰；普通增援门改用 window 策略、取消 256 兵力截断；按真实障碍边界铺木墙。菜单、三关敌人/箱奖励/移动速度/火力参数、存档键和无音乐规则保持。

旧门正值上限为 4/8/12，缺省值为 64；未找到对应原版实证公式，因此只视为旧工程初值。现有 15 个普通门显式使用 window：真实累计伤害达到阈值才加值，在有效射击时间内可继续增长。旧 capped 配置兼容保留；48 个加权显示单位不是兵力上限；数值安全保护为 2^42，属于异常保护，不是正常玩法封顶。详细依据、源行号、A/B、窗口与领取量见封顶报告。

截图对照是可复现的同状态夹具，显式固定旧 capped 规则隔离视觉差异；自然录像使用新的 window 规则和正常触摸输入。定向兵器展示另有标注，不能混称自然单局。代码回归、素材完整、开发端视觉、用户审美及手机结果分别记录。

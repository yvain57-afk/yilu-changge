# 直接执行：《一路长歌：三国》v0.6 兵海、双路、兵法

你在现有Cocos工程上继续开发。用户要看实际效果，这是一份开发授权，不是再次征求策划意见。

## 任务
完成一次连续带兵闯关：越来越多的真实敌兵迎面接近，途中三次左右分路，每处有“稳进／强攻”，武器与兵法搭配影响清敌效率；途中击杀升阶，最后仍挑战同一个本关Boss。

同一批次完成现有三关：乱军突围／邢道荣、夺粮立营／陈应、白石解围／杨龄。不新增第四关，不改世界观或已认可人物。先在第一关内部验证，再复用到后二关，不逐项等用户审批，不停在灰盒或文字报告。

依次阅读：01_SPEC.md → 02_ENGINE_CONTRACTS.md → 03_BOSS_AND_VISUAL.md → 04_ACCEPTANCE.md。两份config是试制初值；contracts是可运行的独立算法参考，不是可以覆盖现有model.ts的游戏实现。tools/check_package.mjs只检查本包，不代表游戏通过。

## 工作区与最新边界
默认工程：/Users/yvainair/Code/游戏-左右滑古代史。
本包核对的远端提交为7ee7685e4bcb4fb7aa67284e7b56692826b86675（v0.5.1）。实际本地可能更新，尤其UI设计agent可能已有成果：先读适用AGENTS.md、当前PROGRESS和实际git diff，保留最新本地修改。禁止checkout/reset/clean/stash覆盖用户成果。提交、推送、公开部署、付费、账户变更不包含在本轮授权。

下面几条新玩法明确替代旧产品限制：
- 静态稀疏敌兵改为可移动真实敌群；关卡中加入分路。
- 旧两名精锐直接升阶，替换为整队实际击杀30／80升阶。
- 普通兵不再套用每次攻击仅1／1／2目标上限；同目标只伤一次、近战与波共账本、地形阻挡仍保留。
- 原有静态门事件按新表重排，不把旧门和新路段叠加生成。
其他已有近战接触、齐射、连续背景、三关流程、存档、队友、六秒觉醒、音效、禁BGM保持。

## 直接做，控制投入
复用现有敌兵、武器动作、Boss和UI组件。只增加必要攻击特效/路牌/两种兵法图标，不重选A/B，不做全武将、装备经济、随机地图、主动技能栏或大型重构。
路口提示须适配刘海与微信胶囊；若UI agent已交付，沿用其safe rect和组件，不覆盖它的首页。没有对应封装时，先在现有Platform/UI边界接一个小适配，不写死刘海高度。
BGM继续零加载零播放，现有音效限流复用。

## 验证与输出
本轮不用旧verify-v03、十局长测或每次游客微信构建。先做改动相关模型测试；一次短密度检查；最终最多一次自然三关原速现场音效录像并复用。四种武器兵法组合用离线模型检查，加两段同一路口短片；不要四种组合各录完整三关。
模型控制器只能发合法移动目标；隔离夹具与正常关卡明确标注。失败只重跑相关检查。完整日志留文件，对话只报关键结果。
微信预览在最终Web通过后只生成一次；使用本地已授权测试身份。登录需扫码就请用户在官方窗口做，其他工作继续，不借用其他AppID、不发布。

最后交付：可玩的三关版本、两段清群/分路差异短片、一次Boss招式范围对照、原速三关录像、测试简报。说明哪些是真机看到的、哪些仅模型/浏览器。不把测试绿灯称为用户审美通过。

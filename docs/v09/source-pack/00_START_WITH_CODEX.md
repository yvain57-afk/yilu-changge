# 直接执行：v0.9 全局体验重整

你在现有《一路长歌：三国》工程内实施一次完整产品优化。用户最新要求是“全局优化”，本任务取代上一轮“只给最小修复”的范围限制。不要再把任务收缩成消除双主角、改几个文字坐标后结束。

## 先读，随后开始实施
1. `01_GLOBAL_DESIGN.md`：完整产品的目标形态与所有页面要求。
2. `02_IMPLEMENTATION.md`：实际模块、渲染生命周期、战斗可读性、参数与资源的接入合同。
3. `03_ACCEPTANCE.md`：一次交付范围与按改动验证。
4. `04_EVIDENCE.md`、`references/`：v0.8依据与原始问题；图片是问题证据，绝不是已批准设计稿。
5. `design_tokens.json`、`review_states.json`：设计初值与状态覆盖，按内容接入，不要求原样复制代码结构。

继续当前本地最新累积工作区；读取适用AGENTS、PROGRESS并核对版本。参考快照为0.8.0-rc1、runnerVideoV2，包含未提交UI成果。不得用旧GitHub覆盖本地，不reset/clean/stash未知改动，不重建另一个游戏工程。

## 一次交付的目标
完成首页、关卡、行军整备、营地、关间过渡、会面、战斗HUD、世界标签、胜利/失败、暂停/设置及加载错误态的统一。连同队伍、敌我接触、门箱、弹道反馈、道路/地景、三关节奏和包体一起收尾。

设计方向沿用已认可的三国手绘角色，建立“军营出征—连续跑道—战后落脚”的一致体验。可以重排版、重组显示节点、修改阵型与必要素材，允许有记录的接触/节奏优化。不要把“保留成果”解释成不许改变有问题的显示方式。

核心玩法仍对齐用户提供的Last War左右移动实录：持续横移即分配火力、可射击改值的正负门、耐久与奖励分离、连续+1门、多列目标、占位风险和后段敌将。不要恢复独立难易分路、固定×2为核心、击杀30/80升级、强制近战决斗或另一套养成。

## 执行方式
先整理显示层和资源，再把一套首页/整备/战斗的共用样式在真实Cocos中接通，随即扩展全部页面和三关。设计与实现可以迭代，但这是内部顺序，不是逐次等待用户批准的停点。不要再次出四套A/B，不要只交概念图或新策划。

有其他设计agent的较新成果时先检查实际文件，接入兼容部分；不能假称已获审批，也不能让等待其输出成为整批停点。多agent仅在可用时采用明确文件归属：UI/资产、runner接触/节奏、平台/资源各自独占文件，主执行者整合共享入口；不得同时编辑Game.ts。

## 约束
- 仍只有三关，保留用户存档、解锁、设置、原best和runnerBest；不新增资源经济、抽卡、经营、商业化。
- BGM不加载不播放。保留原音效，修复音量峰值/密集触发；不在本轮选曲。
- 真实模型驱动画面。不得删活敌、假减人数、动态缩HP、偷偷扩大碰撞或用动画改伤害。
- 不触碰现有权限和发布边界；扫码、付费、公开发布仍需用户授权。已有专用测试身份可继续核验并生成测试预览，不借其他业务AppID。
- 路径与未知用户工作保护继续有效。构建和素材整理不得删除源文件来隐瞒问题。

## 验证与输出
修改过程中只运行相关检查。最终必要构建后，一次自然三关连续流程兼作录像；UI状态夹具和15—25秒密度片段只补该流程覆盖不到的情况，不默认旧十局长测。历史错误保留，长日志落盘，对话不整段贴出。

交付可玩的完整版本、已实测的本机启动入口、当前微信测试预览或具体阻塞、一次原速现场音效录像、同状态前后对照、精简变更/待验清单。不要拿“若干测试全绿”代替画面和手感结果。

完成定义：这一轮必须同时出现“页面像一套产品、战斗看得清、奖励反馈完整、跑道节奏连贯、手机能操作”的可见变化。局部bug消失只是其中一部分。

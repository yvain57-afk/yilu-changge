# 给 Codex：v0.5 武器战气、队友与局内成长

直接在当前最新的本地 v0.4 工作区实施，先只读核对版本和已有修改，不回退。GPT 在 2026-09-22 读取远端 main 仍为 a8b56c16cc94f26d81fe12479b3d12854989efd8，docs/v04/GPT_REVIEW.md 返回404；远端旧代码不能覆盖本地已经完成的 v0.4。

按 01_DESIGN_AND_IMPLEMENTATION.md 实施，02_CANDIDATE_CONFIG.json 是规划数据，必须适配本地模型，不能当作已经可编译的配置直接覆盖。03_CAST_AND_SOURCES.md 只供开发查证，不进入正常游戏UI。

这一轮连续交付三项：
1. 关闭并停止加载背景音乐，保留现有音效；主将长枪/长刀均有真正向前移动的武器波，近处保留武器接触，不以红色矩形冒充攻击。
2. 同一局武器三级成长及一次六秒“破阵觉醒”；三关后依次解锁邢道荣、陈应、赵云同行助阵，一个随军队友位置，队友真实参战。
3. 现有山道/营寨/城门改为零陵外围/桂阳一线/长沙方向的白石驻地；三个Boss分别邢道荣大斧、陈应飞叉、杨龄长枪。保留三关、白石落脚而非占领整个荆州的成长尺度；保留整军—建营—驻防操作，奖励嵌入同一过渡。

新要求覆盖旧的“普通攻击不发冲击波”约束；已经完成的近战不删除。新机制会改变输出，必须做新机制相关检查，不能声称与旧模型逐事件伤害完全一致。兵力上限256、可见48人含主将及随军队友、固定+N/×2门、每排一次、零兵失败、预告、无超时、免费重开、存档保护继续有效。

制作次序：先静音BGM和完成枪/刀同屏战斗，再接三种Boss和队友，最后把升阶/觉醒/战后奖励接进三关。内部自查并连续推进，不重新选A/B，不逐项等待批准。图集、同一角色的敌我状态尽量共享；新增武器、动作和特效必须真实接入，不能以立绘或原理图交差。

不新增第四关、完整武将库、抽卡、装备经济、经营、付费、联网或公开发布。微信测试号按本地当前状态核实；需要登录由用户扫码，不读取或索取凭证。Web成果不因微信登录阻塞。

只读必要文件；日志落盘，对话只报关键结果。局部检查→一次快速规则/类型→必要Web构建→一个有界特效密度检查→一次自然三关原速有声录像（只有音效）并复用。不要默认运行旧verify-v03、十局长测、重复全流程录屏或无变更的双平台构建。既有测试保留，变化的产品合同用明确的迁移测试替换，不删断言凑绿。

最后提交可玩的三关、简短变更记录和同版验证结果。必须区分：真实试玩、隔离夹具、参数估计；不因测试通过宣布审美或微信真机验收通过。

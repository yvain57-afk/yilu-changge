# FIX2 素材记录

12次内置 imagegen 生成/编辑（其中1次由父代理完成），所有原图和失败尝试保留。7张运行图集、28个帧键（27必需＋hitFlash），总 524653 bytes。纹理解码估算 5052008 bytes（宽×高×4，不是GPU实测）。

- blade：三阶段关节动作；Release刀刃跨身体轴。
- actions：枪主将、邢道荣重斧、陈应三叉飞叉、赵云枪，各三阶段；Release中心金属点实测。只Release有muzzle，其余不适用。
- waves：刀弧、枪芒、重斧波、三叉波、枪口短闪、命中短闪；完整RGBA渐变。
- environment：父生成的直向木墙中段/首尾、三色低噪旗面，9slice边距8像素。
- zhao：独立三帧修复完整靴底与透明留边；Release实测金属x与双脚接地点中轴一致。旧赵云三帧保留在历史原图/原图集中，已无活动key引用。
- shadow：独立半透明墙影，最大alpha114。

连通域切片解决陈应脚与下一行枪尖跨名义格的问题，提取alpha>0连通像素，不绘制或补写人物。角色运行图经过palette256压缩；波/墙/影使用RGBA。来源、SHA、原始尺寸、显式裁切和插入点见 runtime-manifest.json 与 specs。

已修复缺口：赵云原sheet少量靴底截断，经第11次内置生成编辑，独立三帧全部保留完整靴底，主体距图片底边至少31px。Release双脚接地点读取(982,685)/(1190,603)，平均(1086,644)；金属发射点(1086,82)，126显示高时socket=(0,104.1353)。各帧足点均按前后脚实际接地点平均，而非单脚最低边。原图和旧runtime继续保留。

实际场景视觉与全包大小由后续构建核验，本文件不宣称最终场景验收通过。

第12次生成修复墙中段：独立 wall-middle.png（40×328，19,486 bytes），有效帧32×320，仅替换 wallMiddle。上下贯通梁中轴原图差0.5px（运行缩放后<0.1px），不再含原斜移/渐窄；组装接缝仍需主流程实际截图确认。旧 environment 图保持不变，旧中段 key 已标 inactive。
